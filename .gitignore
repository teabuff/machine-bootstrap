# Local agent / editor workspace and private design notes — never commit
.claude/
docs/superpowers/

# Per-host declarative manifests (real ones carry private values). Only the
# example template is tracked.
provisioning/manifests/*.host
!provisioning/manifests/example.host

# Newt site connector — local secrets/credentials/state
newt-site/.env
newt-site/newt-data/
